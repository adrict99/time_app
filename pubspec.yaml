name: time_app
description: A new way of using your phone

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  table_calendar: ^2.3.1
  firebase_auth: ^0.18.3
  firebase_core: ^0.5.2
  cloud_firestore: ^0.14.3
  google_sign_in: ^4.5.6
  modal_progress_hud: ^0.1.3
  flutter_icons: ^1.1.0
  cupertino_icons: ^0.1.3
  shared_preferences: ^0.5.12+4
  intl: ^0.16.1
  percent_indicator: ^2.1.8
  audioplayers: ^0.17.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
